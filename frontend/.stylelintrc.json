{
  "extends": "stylelint-config-standard",
  "plugins": ["stylelint-order"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "custom-variant", "utility", "plugin", "config", "tailwind", "apply", "layer"]
      }
    ]
  }
}
